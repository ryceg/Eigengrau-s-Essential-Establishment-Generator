:: NPCProfile
<h1>$currentNPC.name</h1>
$currentNPC.name is a $currentNPC.malefemale $currentNPC.race. <<print $currentNPC.heshe.toUpperFirst()>> is $currentNPC.height and $currentNPC.weight, and has $currentNPC.eyes eyes, with $currentNPC.skinColours skin. The most notable physical trait of $currentNPC.firstname is $currentNPC.hisher $currentNPC.physicaltrait.

$currentNPC.firstname is a $currentNPC.dndclass. <<print $currentNPC.hisher.toUpperFirst()>> background is $currentNPC.background. $currentNPC.firstname currently has $currentNPC.pockets in $currentNPC.hisher pockets.<div class='descriptive'>"$currentNPC.backgroundOrigin. $currentNPC.dndclassOrigin. That's how I became a $currentNPC.dndclass. $currentNPC.bond"</div>



<<return [Return]>>

:: ChemistTalk

$associatedNPC.firstName looks <<print $associatedNPC.currentMood.random()>>, and idly shifts a box of <<print $associatedNPC.ingredients.random()>> as $associatedNPC.heshe talks. The $associatedNPC.raceNote <<print $associatedNPC.chitchat.random()>> as you peruse the shop. $associatedNPC.firstName tells you that $associatedNPC.heshe is working on <<print setup.articles.output($brew.potionPurpose)>>, and points to the <<linkappend $brew.containerDescription>>. Looking inside the $brew.vesselType, you see <<print setup.articles.output( $brew.liquidDescription)>> bubbling away<</linkappend>>.

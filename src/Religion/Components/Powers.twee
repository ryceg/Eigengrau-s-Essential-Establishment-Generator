:: Powers
<<if $currentPassage.blessings || $currentPassage.blessings || $currentPassage.curses>><h2>Powers</h2><</if>>
\<<if $currentPassage.powers>><<print lib.printInformation($currentPassage.powers)>><</if>>
\<<if $currentPassage.blessings>><h4>Blessings</h4><<print lib.printInformation($currentPassage.blessings)>><</if>>
\<<if $currentPassage.curses>><h4>Curses</h4><<print lib.printInformation($currentPassage.curses)>><</if>>
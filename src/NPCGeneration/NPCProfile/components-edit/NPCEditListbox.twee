:: NPCEditListbox
<<silently>>
    <<set _class to '<<listbox "$npcs[$currentPassage.key].profession" autoselect>><<optionsfrom Object.keys(lib.professions)>><</listbox>>'>>
    <<set _background to '<<listbox "$npcs[$currentPassage.key].background" autoselect>><<optionsfrom Object.keys(lib.backgroundTraits)>><</listbox>>'>>
    <<set _race to '<<listbox "$npcs[$currentPassage.key].race" autoselect>><<optionsfrom Object.keys(lib.raceTraits)>><</listbox>>'>>
    <<set _gender to '<<listbox "$npcs[$currentPassage.key].gender" autoselect>><<optionsfrom Object.keys(lib.genderData)>><</listbox>>'>>
    <<set _replaceNpc to $npcs[$currentPassage.key]>>
<</silently>>\